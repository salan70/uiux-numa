---
title: 状態とフィードバック
summary: 操作の結果と状態変化を即座に伝え、レイアウトシフトを起こさず安定した挙動を保つ。
status: draft
axes:
  - interaction clarity
  - feedback quality
  - motion appropriateness
created: 2026-09-20
updated: 2026-09-20
---

## 目的

操作に対するシステムの応答と現在の状態を、迷いなく利用者に伝える。
状態の変化で画面の配置を崩さず、安定した操作感を維持する。

## 適用範囲

ボタン、タブ、入力検証、進行状況表示、通知などの状態遷移に適用する。
動きの設計は crafting-motion を正本とし、本稿では原則のみ扱う。
動きを減らす端末設定への追従はアクセシビリティのガイドラインが扱う。

## 規則

### 状態は色や面や線で示し寸法を変えない

意図と根拠: 現在地や選択を太字や余白で示すと、状態が変わるたびに周囲が揺れる。
文字色、背景面、下線で状態を表し、要素の寸法を維持する。

- 良い例: 現在地のタブを文字色と下線だけで示し、太さは変えない。
- 悪い例: 選択されたタブを太字にして幅が広がり、隣のタブを押し出す。
- 例外: アコーディオンの開閉など、コンテンツ自体の展開を目的とする操作。
- 図: state-layout-shift
- 実験: [状態は色で示し、寸法で示さない](../../docs/principles/state-changes-must-not-move-layout.md)

### 内容で幅が変わる場所は最大値で固定する

意図と根拠: 数値や文字数が変わるたびにボタンの幅が動くと、次の押し先がずれる。
想定される最大幅をあらかじめ確保し、押し先を固定する。

- 良い例: 送りボタンの幅を 8rem、数字の表示幅を 4.5rem で固定する。
- 悪い例: 配色名や数字の文字数に合わせてボタンの幅を可変にする。
- 例外: 画面端まで flex で広がる流動的なグリッド配置。
- 実験: [catalog-editorial/rationale/topic-first.md](../../experiments/catalog-editorial/rationale/topic-first.md)

### ホバーで広げる表現は細かいポインタに限る

意図と根拠: タッチ端末でホバー依存の伸縮を使うと、タップ時に意図せぬ画面の揺れを招く。
ポインタの解像度を判別し、マウス環境でのみ控えめに広げる。

- 良い例: pointer: fine かつ hover: hover の環境でのみ帯を広げる。
- 悪い例: タッチ端末でも要素タップ時に周囲の帯が伸縮する。
- 例外: ポインタ種別によらず常時展開して表示する画面構成。
- 実験: [catalog-editorial/rationale/topic-first.md](../../experiments/catalog-editorial/rationale/topic-first.md)

### 操作の結果はその場で即座に返す

意図と根拠: 操作後に無反応な時間があると、利用者は処理が届いたか不安になる。
視覚的な変化やインジケータで、操作の受け付けを即座に知らせる。

- 良い例: 保存ボタンを押した瞬間に無効化と処理中表示へ切り替える。
- 悪い例: 送信を押しても数秒間画面に変化がなく、多重クリックを誘発する。
- 例外: 100ms 未満で完了する軽量なローカル同期処理。
- 実験: [form-inline-validation](../../experiments/form-inline-validation/README.md)
- 出典: [Nielsen Norman Group 10 ヒューリスティクス](https://www.nngroup.com/articles/ten-usability-heuristics/)

### 反応時間は短く保ち頻度の高い操作は動かさない

意図と根拠: 1 日に何度も行う操作で大きなアニメーションを挟むと作業効率を落とす。
出発点は押下 100〜160ms、小さな面 125〜200ms とし、頻度が高いほど短くするか動かさない。

- 良い例: タブの切替や選択を 100〜160ms に収めるか、遷移を置かずに切り替える。
- 悪い例: 頻繁に押すナビゲーションで 400ms のスライドを理由なく待たせる。
- 例外: 達成感や安心感を意図して演出する稀な完了画面。300ms を超えるときは理由を書く。
- 実験: [crafting-motion](../../skills/crafting-motion/SKILL.md)

### エラーは文字で特定し、要約へフォーカスを移す

意図と根拠: 何が誤りかを文字で示すことは WCAG 2.2 達成基準 3.3.1 の要求である。
加えてフォーカスを移さないと、キーボードと支援技術は送信失敗に気づけない。
フォーカス移動は基準の要求ではなく、form-inline-validation で効果を確かめた本リポジトリの追加である。

- 良い例: 送信エラー時に、上部に表示したエラー要約へフォーカスを移す。
- 悪い例: 送信ボタンの位置にフォーカスが留まり、画面上部のエラーに気づかない。
- 例外: 入力中のフィールドの直下でリアルタイムに表示する軽微な警告。
- 実験: [form-inline-validation](../../experiments/form-inline-validation/README.md)
- 出典: [WCAG 2.2 達成基準 3.3.1](https://www.w3.org/WAI/WCAG22/quickref/#error-identification)

### 入力検証は送信時に始め、以後は修正のたびに返す

意図と根拠: 入力の途中や離脱のたびにエラーを出すと、タイピングのリズムを阻害する。
離脱時に検証する on-blur は、表示の高さが変わって修正直後の送信クリックが外れるため form-inline-validation で却下した。
初回の検証を送信時に寄せ、以後は修正のたびに同じ項目だけを再検証する。

- 良い例: 初回は送信時にまとめて検証し、以後は修正のたびにその項目を再検証する。
- 悪い例: フィールドから離脱するたび、または 1 文字目の入力で警告を出す。
- 例外: ユーザー名の重複確認など、即時の確認に明確な価値がある場合。
- 実験: [form-inline-validation](../../experiments/form-inline-validation/README.md)

### 処理の完了を支援技術へ確実に伝える

意図と根拠: フォーム送信後にフォーカスが消えると、支援技術は完了を読み上げられない。
画面を切り替えるなら完了見出しへフォーカスを移し、切り替えないなら `role="status"` の領域へ文言を挿入する。
達成基準 4.1.3 が求めるのは後者、フォーカスを受けない状態メッセージの通知である。

- 良い例: 登録完了画面への切り替えと同時に、完了見出しへフォーカスを移す。
- 悪い例: フォームが消えてフォーカスが body に落ち、読み上げが途絶える。
- 例外: 画面を切り替えない副次的な保存。`role="status"` に文言を入れ、フォーカスは動かさない。
- 実験: [form-inline-validation](../../experiments/form-inline-validation/README.md)
- 出典: [WCAG 2.2 達成基準 4.1.3](https://www.w3.org/WAI/WCAG22/Understanding/status-messages.html)

### エラーの解除は高さを予約したうえで行う

意図と根拠: 正しい入力に直したあともエラーが残ると、修正できたか確信が持てない。
一方でエラー文をそのまま消すと下の要素がせり上がり、「状態は色や面や線で示し寸法を変えない」に反する。
form-inline-validation では、この消去でボタンが 28px 動き on-blur の却下理由になった。
エラー文の領域をあらかじめ確保し、高さを保ったまま文言と赤枠だけを解除する。

- 良い例: エラー文の行を `min-height` で予約し、解除時は文言と枠の色だけを戻す。
- 悪い例: エラー文の要素ごと取り除き、下の送信ボタンをせり上がらせる。
- 例外: サーバーへの問い合わせを伴う非同期検証。結果が返るまで解除しない。
- 実験: [form-inline-validation](../../experiments/form-inline-validation/README.md)

### 動かないことは矩形を測って確かめる

意図と根拠: 数ピクセルのレイアウトシフトは、目視の確認だけでは見落としやすい。
状態変更の前後で要素の座標と寸法を読み取り、完全な一致を検証する。

- 良い例: ブラウザの開発者ツールで状態前後のバウンディングボックスを測る。
- 悪い例: 画面を眺めて「動いていないように見える」だけで判断を終える。
- 図: state-stable
- 実験: [状態は色で示し、寸法で示さない](../../docs/principles/state-changes-must-not-move-layout.md)

## 確認項目

- [ ] タブやボタンの選択切替で、周囲の要素の位置が動いていないか。
- [ ] 送り操作やページネーションで、ボタンの位置が固定されているか。
- [ ] 頻度の高い操作の遷移時間が 100〜200ms に収まっているか。
- [ ] フォーム送信エラー時に、誤りが文字で示され、要約へフォーカスが移動するか。
- [ ] エラーの表示と解除の前後で、周囲の要素の高さが変わっていないか。
- [ ] 状態変更の前後で、隣接要素の座標と寸法が一致しているか。

## 出典

- [Nielsen Norman Group: 10 Usability Heuristics](https://www.nngroup.com/articles/ten-usability-heuristics/): システム状態の可視性（1）
- [WCAG 2.2: 達成基準 3.3.1 / 4.1.3](https://www.w3.org/WAI/WCAG22/quickref/): エラーの特定、ステータスメッセージ

## 判断

- 判断者: 未定
- 判断日: 未定
- 理由: draft のため未定
