---
title: Color
summary: 色を役割名で管理し、役割の意味を配色をまたいで保ち、計算と見本で視認性と調和を確かめる。
status: draft
axes:
  - visual hierarchy
  - accessibility
  - consistency
created: 2026-09-20
updated: 2026-09-22
---

## 目的

配色の意図を崩さず、UI の役割と状態を明瞭に伝える。
無彩色を中心にするか有彩色の面を使うかはプロダクトの作風として選び、本稿は作風によらない品質の条件を定める。
コントラストの閾値と、色だけで意味を伝えない規則は accessibility が正本とする。

## コア

### 色は役割で引く

すべての色を役割名のトークンで引き、値を直接書かない。
指定の手軽さより、配色やテーマを替えても意図が保たれることを優先する。

### 役割の意味は配色をまたいで動かさない

役割名が指す意味を先に定義し、配色ごとに変えるのは割り当てる色だけにする。
配色ごとの最適化より、意味が一定であることを優先する。

### 階層は明度差で作る

面や段の違いは、色相や彩度ではなく明度の差で示す。
色の表情より、環境や色覚によらず伝わることを優先する。

### 比は計算し、調和は見本で確かめる

コントラスト比は目視で決めず計算で確かめる。
調和と階層は計算では決まらないので、色だけの見本と実画面の両方で確かめる。

## Tips

### 色は直接の値ではなく役割名で引く

意図と根拠: 色名や hex 値でスタイルを指定すると、テーマ切替で破綻する。

- 適用: foundation
- コア: 色は役割で引く
- 良い例: CSS 変数 `--color-accent` や `--ed-surface` で指定する。
- 悪い例: スタイルシートに `#165e83` などの固定値を直接記述する。
- 例外: パレットの基礎となる元データを定義する定義体。
- 実験: [catalog-editorial/rationale/topic-first.md](../records/catalog-editorial/rationale/topic-first.md)
- 出典: [Design Tokens Community Group 仕様](https://design-tokens.github.io/community-group/format/)

### 余った色をそのまま残さない

意図と根拠: 画面で使われない色がパレットに残ると、選択に迷いが生じる。

- 適用: foundation
- コア: 色は役割で引く
- 良い例: 役割に割り当てられた必要な色のみを定義する。
- 悪い例: 生成ツールが作った無関係な濃淡スケールを全件保持する。
- 例外: 画面に出さない参照用の定義体。配色の元にした伝統色の一覧など。
- 実験: [color-schemes](../records/color-schemes/README.md)

### 意味色は成功、警告、エラー、情報の 4 つを既定にする

意図と根拠: 意味を持つ色が増えるほど、利用者は対応を覚えきれない。
4 つは通知に使う既定の分類であり、上限ではない。
足すときは役割を定義し、色以外の語と形でも区別する。

- 適用: foundation
- コア: 役割の意味は配色をまたいで動かさない
- 良い例: 4 つの意味色を全画面で同じ意味に使う。
- 悪い例: 状態ごとに 9 色を割り当て、画面ごとに対応を変える。
- 例外: データの可視化。系列の区別に別の体系を使う。

### 意味色は配色をまたいで意図を揃える

意図と根拠: 成功やエラーの意味色が配色ごとに変わると、状態を誤読する。

- 適用: foundation
- コア: 役割の意味は配色をまたいで動かさない
- 良い例: 全配色でエラーに赤系、成功に緑系の色を割り当て、accent は主たる操作に割り当てる。
- 悪い例: ある配色だけ accent を装飾に使い、エラーの色を紫や青に変える。
- 例外: OS 標準のセマンティックカラーに委ねる場合。
- 実験: [color-schemes](../records/color-schemes/README.md)

### 地、面、浮いた面は明度の段差で分ける

意図と根拠: 色相や彩度の違いは、環境や色覚によって読み取れないことがある。

- 適用: foundation
- コア: 階層は明度差で作る
- 良い例: 地、面、浮いた面を明度の段差で分ける。
- 悪い例: 同じ明度の別色相で面を分ける。
- 例外: 明度を揃えることが目的の、色相の比較見本。

### コントラスト比は計算して確かめる

意図と根拠: 人間の目は周囲の明度によってコントラストを誤認しやすい。
満たすべき比の値は accessibility が定める。本稿は確かめ方を扱う。

- 適用: foundation
- コア: 比は計算し、調和は見本で確かめる
- 良い例: 計算ツールを用いて 4.5:1 や 3:1 の達成を数値で確認する。写真やグラデーションの上では、文字の背後で最も不利な部分を測る。
- 悪い例: 画面を目視して「読めそうだから問題ない」と判断する。
- 実験: [apps/catalog/src/content/contrast.ts](../../apps/catalog/src/content/contrast.ts)
- 出典: [WCAG 2.2 達成基準 1.4.3](https://www.w3.org/WAI/WCAG22/quickref/#contrast-minimum)

### 画面に組むより先に色だけの見本で判断する

意図と根拠: 複雑な UI に適用してからでは、造形と色の影響が混ざり合う。

- 適用: foundation
- コア: 比は計算し、調和は見本で確かめる
- 良い例: パレットカードや色帯を並べ、相性と明度差を先に確認する。
- 悪い例: 完成した画面にいきなり色を当てて全体の可否を議論する。
- 例外: 微小な文字サイズにおける個別の視認性確認。
- 実験: [catalog-editorial/README.md](../records/catalog-editorial/README.md)

### 無彩色を中心にする作風では、有彩色を操作と状態に限る

意図と根拠: 有彩色の面積が増えるほど、1 つあたりの強さは弱まる。
これは作風の選択であり、有彩色の面そのものが視認性を落とすのではない。
文字との比と役割の識別を保てば面に色を使ってよい。

- 適用: module
- コア: 色は役割で引く
- 良い例: 背景を無彩色の白鼠や墨色にし、ボタンのみにアクセント色を使う。
- 悪い例: カードの面やヘッダー全体を有彩色で塗りつぶす。
- 例外: ブランドの告知など、強い情緒表現を意図する案内面。
- 実験: [catalog-editorial/rationale/topic-first.md](../records/catalog-editorial/rationale/topic-first.md)

### 見本帳は固有のブランド色を持たない

意図と根拠: 見本帳自身が固有の色を持つと、展示する配色の見え方を歪める。

- 適用: module
- コア: 色は役割で引く
- 良い例: カタログの面の色を正本の配色定義から直接導く。
- 悪い例: カタログ固有のアクセントカラーをヘッダー等に残す。
- 例外: 成果物の表示と完全に独立したシステム管理バー。
- 実験: [docs/decisions/2026-09-19-catalog-host.md](../../docs/decisions/2026-09-19-catalog-host.md)
