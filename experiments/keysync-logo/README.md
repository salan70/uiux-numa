---
title: KeySync のロゴ
status: decided
role: module
maturity: experimental
created: 2026-09-25
updated: 2026-09-25
platforms:
  - web
domains:
  - logo-brand-identity
  - visual-design
  - accessibility
sources:
  - uiux-numa-logo
  - cornix-ui-icons
  - color-schemes-material
adopted:
  - tilt-confetti
---

## Problem

Cornix Bonsai は 2026-09-25 に KeySync へ改名した（Issue #15、KeySync の ADR 0035）。
Cornix LP だけでなく、MacBook の内蔵キーボードと Magic Keyboard、今後買う機種のキーマップを 1 か所で管理する道具にするためである。
ロゴは絵文字 🌱 だけで、SVG のマークも favicon も無い。
🌱 は旧名の bonsai から来ており、新しい名前とも道具の役割とも結び付かない。

## Target

KeySync を macOS の Chrome で使い、複数のキーボードの割り当てを編集する個人と、その友人。
ロゴが出る面は 3 つある。
最も頻度が高いのはブラウザのタブ（16px）である。
次がヘッダーの題字（黄のキーキャップ型の箱 36px の中）、最も大きいのが workspace の入口（箱 64px）である。

## Scope / Domains

対象はマーク 1 個と、その lockup の形式である。
domain は `logo-brand-identity`、`visual-design`、`accessibility`。
`logo-brand-identity` を先頭に置き、Catalog に載せない。

比喩の系統と目指す印象は、制作前に利用者が決めた（2026-09-25）。

- 比喩（第 1 世代）: 4 系統すべてで下書きを作る。キーキャップが揃う、文字 K を核にする、同期の矢印や円環、キーボードの盤面を抽象化する。
- 比喩（第 2 世代）: 第 1 世代の 10 案から、利用者が `board-pop`（盤面から 1 個のキーだけが浮く）の方向を選んだ（2026-09-25）。その派生を作る。
- 比喩（第 3 世代）: 第 2 世代から利用者が `pop-tilt` と `pop-confetti` を良いとした（2026-09-25）。あわせて「キーが K や S に見える」か「通常の配列の K と S の位置の色が違う」案を求めた。
- 印象: カラフルで楽しい。Pop Toy 配色と同じ方向で、丸みのある形と太めの面を使う。
- 色: 単色（`currentColor`）を基本にし、Pop Toy の多色版を同じ形で添える。

第 3 世代は、`pop-tilt` の姿勢と `pop-confetti` の色を合わせた `tilt-confetti` を基準にする。
変える軸は、K と S の示し方（色を置く位置、盤面の並び、浮いたキーの刻印）の 3 つで、1 案に 1 つずつ割り当てる。
第 2 世代の 2 案は比較のために残す。

## Constraints

- `<text>`、`var()`、`aria-labelledby` を使わない。`role="img"` と最初の子 `<title>`、id は `part-mark-<role>`、色は `currentColor`（[SVG ツールチェーンの ADR](../../docs/decisions/2026-09-18-svg-toolchain.md)）。
- 白黒 1 色で成立させてから色を考える（`LOGO-07`）。多色版は SVG に色を書かず、part の id の末尾で役割を示し、利用画面の CSS が塗る。`-accent` は黄（primary）、`-accent-2` は青（secondary）、`-accent-3` は赤橙（tertiary）。
- `-accent` は塗りの part だけに付ける。`just svg-compare` は `-accent` の fill だけを塗るためである。
- 黄は白の面に 1.61:1 しかない。多色版で黄の面だけが形を担う案は、明の面で輪郭が弱くなる。
- 本体のヘッダーと入口では、マークは黄の箱（`.logo`）の中に置かれる。単色版は箱の上で黒に近い `on-primary` になる。多色版を使うなら箱を外す。
- viewBox は `0 0 32 32`、live area は `4..28`。favicon の基準が 32px である（`LOGO-07`）。
- Catalog には載せない（[掲載しない Experiment の ADR](../../docs/decisions/2026-09-22-unlisted-experiments.md)）。

## Hypothesis

盤面から 1 個のキーだけが傾いて浮く形は、キーボードを扱う道具であることと、割り当てを変える操作を 1 つの図で示せる。
そこに名前の頭文字 K と S を、色の位置、キーの並び、刻印のどれかで重ねると、図形の比喩のまま名前とも結び付く（`LOGO-02`、uiux-numa-logo の Learnings）。

## Variants

2026-09-25 に利用者が `tilt-confetti` を採用した。
採用後は `tilt-confetti` だけを残す。ほかの案と一覧面 `overview` は「削除した variant」に仮説と軸を残した。

| id              | 仮説                                                         | 変えた軸   | 実装                      |
| --------------- | ------------------------------------------------------------ | ---------- | ------------------------- |
| `tilt-confetti` | 傾きと色の散らし方を合わせると、動きとカラフルさを両方持てる | 2 案の合成 | `variants/tilt-confetti/` |

原本は `shared/build-marks.mjs` が書き出す。配布用は `just svg-optimize` の出力である。

### 造形のパラメータ

採用した `tilt-confetti` の値である。盤面は第 2 世代の基準 `board-pop` から、浮いたキーの傾きは `pop-tilt` から、色の配り方は `pop-confetti` から引き継いだ。

| 項目      | 値                                         | 理由                                                                        |
| --------- | ------------------------------------------ | --------------------------------------------------------------------------- |
| viewBox   | `0 0 32 32`                                | favicon の基準が 32px（`LOGO-07`）。uiux-numa-logo と同じ                   |
| live area | `4..28`（24 角）                           | 外周 4 を余白に残す（`LOGO-06`）                                            |
| 格子      | 偶数座標                                   | 16px（1/2 倍）で整数ピクセルに載る（`SVG-01`）                              |
| キー      | 4 角、角丸 1.25、隙間 2                    | 16px で 2px のキーと 1px の隙間。角丸は一辺の約 3/10 でキーキャップに寄せる |
| 盤面      | 4 列 × 3 段、3 段目の中央は幅 10           | 最も少ない段数でキーボードと読める量。スペースバーで段の向きを示す          |
| 色数      | 1（currentColor）+ 役割 3 つ               | 単色で成立させ、多色は利用画面の CSS で塗る                                 |
| 色の配置  | 浮いたキーだけ黄、盤面は青 3、赤橙 3、墨 4 | 黄を主役の 1 個に限り、盤面は隣り合うキーが同じ色にならないよう散らす       |
| 対称軸    | なし                                       | 浮いたキーを 3 列目に置き、楽しさと動きを出す（`LOGO-03`）                  |

### 座標の導出

`tilt-confetti` の盤面（`board-pop` から引き継ぐ）

| 対象             | 体系                                 | 理論値        | 採用値        | 誤差 | 理由                                                     |
| ---------------- | ------------------------------------ | ------------- | ------------- | ---- | -------------------------------------------------------- |
| 列の左端         | 幅 22（4 × 4 + 2 × 3）を 24 の中央へ | 5, 11, 17, 23 | 4, 10, 16, 22 | −1   | 奇数座標は 16px で半ピクセルにかかり滲む。格子を優先した |
| 段の上端         | 下端を live area の 28 に合わせる    | 12, 18, 24    | 12, 18, 24    | 0    | 浮いたキーの分だけ上を空ける                             |
| 浮いたキーの上端 | live area の上端                     | 4             | 4             | 0    | 元の位置（12）との間は 4。16px で 2px の空き             |
| スペースバー     | 2 列目の左端から 3 列目の右端まで    | 10..20        | 10..20        | 0    | 3 段目の両端のキーとの隙間を 2 に保つ                    |

`tilt-confetti` の浮いたキー（`pop-tilt` から引き継ぐ）

| 対象       | 体系                                          | 理論値 | 採用値 | 誤差 | 理由                        |
| ---------- | --------------------------------------------- | ------ | ------ | ---- | --------------------------- |
| 傾き       | 跳ねる途中に見え、角が live area を越えない角 | −15°   | −15°   | 0    | 4 角の外接が ±2.73 で収まる |
| 回転の中心 | 上端を live area の 4 に合わせる              | 6.73   | 6.75   | 0.02 | 0.25 刻み。上端は 4.02      |

### 反復の記録

| round | 観察                                                                                       | 変更                                                              | 参照      | 終了理由         |
| ----- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- | --------- | ---------------- |
| 1     | `caps-pair` の刻印を下寄せの横棒（ホームポジションの突起）にすると、目と口の顔に読めた     | 刻印を左上の縦の刻みへ移す                                        | `LOGO-02` | 口の読みが消えた |
| 2     | `caps-pair` は左右対称のままでは 2 つの目に読めた                                          | 幅を 9 と 12 に変えて対称を崩す                                   | `LOGO-03` | 上限に達した     |
| 3     | `caps-pair` は幅を変えても目の読みが残った                                                 | 変更なし。系統の選定に持ち込む                                    | `LOGO-02` | 未解決として残す |
| 1     | `k-sync` の入る矢印の矢じりが K の分岐点に重なり、16px で塊になる                          | 変更なし。系統の選定に持ち込む                                    | `LOGO-07` | 判断待ち         |
| 1     | `board-link` は 2 段と縦棒が H や梯子に読める                                              | 変更なし。系統の選定に持ち込む                                    | `LOGO-02` | 判断待ち         |
| 1     | `caps-grid` は 4 個の角丸が汎用のアプリ一覧の記号に近い                                    | 変更なし。系統の選定に持ち込む                                    | `LOGO-04` | 判断待ち         |
| 4     | 利用者が第 1 世代から `board-pop` の方向を選んだ                                           | 骨格を保ち、1 軸ずつ変えた派生 7 案を作る                         | `LOGO-04` | 前提が変わった   |
| 4     | `board-pop` の浮いたキーが y = 3 で、live area の上を 1 越えていた                         | 段を 1 下げ、浮いたキーを y = 4 に置く                            | `LOGO-06` | 越えが消えた     |
| 4     | 奇数座標のキーが 16px で半ピクセルにかかり、盤面が灰色に滲んだ                             | 格子を偶数座標へ移す。横の中心が左へ 1 ずれる                     | `SVG-01`  | 滲みが消えた     |
| 4     | `pop-bold` は 2 個のキーとスペースバーが目と口に、浮いたキーが帽子に見え、顔に読める       | 変更なし。判断に持ち込む                                          | `LOGO-02` | 判断待ち         |
| 4     | `pop-ghost` の輪郭は 16px で 1px 未満になり、ほぼ見えない                                  | 変更なし。32px 以上での差として判断に持ち込む                     | `LOGO-07` | 判断待ち         |
| 4     | `pop-dots` の粒は 16px で 2px の正方形になり、基準と見分けられない                         | 変更なし。24px 以上での差として判断に持ち込む                     | `LOGO-07` | 判断待ち         |
| 5     | 利用者が第 2 世代から `pop-tilt` と `pop-confetti` を良いとし、K と S を読ませる案を求めた | 2 案を合わせた基準と、K と S の示し方を 1 つずつ変えた 3 案を作る | `LOGO-02` | 前提が変わった   |
| 5     | `tilt-confetti` と `ks-home` は単色では `pop-tilt` と同じ形になる                          | 変更なし。多色版の差として判断に持ち込む                          | `LOGO-07` | 判断待ち         |
| 5     | `k-grid` は 16px で 3px のキーが 6 個並び、K より賽の目に近く見える                        | 変更なし。24px 以上での差として判断に持ち込む                     | `LOGO-02` | 判断待ち         |
| 5     | `k-legend` の K は 16px と 24px で潰れ、32px 以上で読める                                  | 変更なし。favicon 専用版の要否と合わせて判断に持ち込む            | `LOGO-07` | 判断待ち         |
| 6     | 利用者が第 3 世代から `tilt-confetti` を採用した                                           | ほかの案と一覧面を削除する                                        | —         | 採用             |

### 削除した variant

2026-09-25 に利用者が第 1 世代の 10 案から `board-pop` の方向を選んだ。ほかの 9 案はコードを削除し、仮説と軸だけを残す。

- `caps-pair`: 幅の違う 2 個のキーに同じ刻印を置く。軸は比喩（キーが揃う）。2 つの目の読みが残っていた。
- `caps-grid`: 2×2 のうち 3 個が揃い、1 個が輪郭で残る。軸は比喩（キーが揃う）。
- `caps-stack`: 奥のキーの輪郭に手前のキーの面を重ねる。軸は比喩（キーが揃う）。
- `k-cap`: キーキャップの天面に K を抜く。軸は比喩（文字 K）。
- `k-sync`: K の斜画を出る矢印と入る矢印にする。軸は比喩（文字 K）。
- `k-keys`: 縦長のキーと傾けた 2 個のキーで K を組む。軸は比喩（文字 K）。
- `ring-cap`: 中央のキーを 2 本の円弧の矢印が巡る。軸は比喩（同期の記号）。
- `swap-caps`: 対角の 2 個のキーを往復の矢印で結ぶ。軸は比喩（同期の記号）。
- `board-link`: 2 台の段を 1 本の縦長キーが貫く。軸は比喩（盤面）。

2026-09-25 に利用者が第 2 世代から `pop-tilt` と `pop-confetti` を良いとした。基準とほかの 5 案はコードを削除し、仮説と軸だけを残す。

- `board-pop`: 第 2 世代の基準。第 1 世代の案を live area と偶数格子に収めた版。`tilt-confetti` へ引き継いだ。
- `pop-motion`: 浮いたキーの左右に短い線を添える。軸は動きの示し方。
- `pop-ghost`: 空いた位置に細い輪郭を残す。軸は元の位置の示し方。16px で輪郭が見えなかった。
- `pop-bold`: 大きいキー 2 個とスペースバーにする。軸は盤面の粒度。顔の読みが残っていた。
- `pop-stagger`: 2 段目を右へ 2 ずらす。軸は段のずれ。
- `pop-dots`: キーを丸い粒にする。軸はキーの形。16px で基準と見分けられなかった。

2026-09-25 に利用者が第 3 世代から `tilt-confetti` を採用した。ほかの 5 案と一覧面はコードを削除し、仮説と軸だけを残す。

- `pop-tilt`: 浮いたキーを -15° 傾ける。軸は浮いたキーの姿勢。`tilt-confetti` へ引き継いだ。
- `pop-confetti`: 盤面のキーを 3 色と墨に散らす。軸は色の配り方。`tilt-confetti` へ引き継いだ。
- `ks-home`: ホーム段の S と K の位置だけ色を変える。軸は色を置く位置。
- `k-grid`: 3×3 の 6 個のキーで K を組み、上の腕だけ浮かせる。軸は盤面の並び。キー 6 角、ピッチ 8、浮いたキーの中心 (24, 7.75)。
- `k-legend`: 浮いたキーを 8 角に大きくして K を刻む。軸は浮いたキーの刻印。K は一辺 4.5 で、16px と 24px では潰れた。
- `overview`: 候補を 1 ページに並べる一覧面。比べる候補がなくなったので、`shared/Overview.tsx`、`shared/candidates.ts`、専用の CSS とともに削除する。

### previews

- `compare-first.html`: 第 1 世代の 10 案の SVG をそのまま並べた比較ページ。16 / 24 / 32 / 64px、明暗、Pop Toy の `-accent`。
- `compare-pop.html`: 第 2 世代の 8 案（基準を含む）の比較ページ。同じ条件。
- `compare-ks.html`: 第 3 世代の 6 案の比較ページ。左から 3 番目が採用した `tilt-confetti`。同じ条件。

削除した案の形は、これらのページに残る。

## Evaluation

評価を行わずに判断した。
利用者が開発サーバーの一覧面（`variants/overview/`）と比較ページで見比べて選んだ。
一覧面で見た条件は、16 / 24 / 32px の単色、ヘッダーの黄の箱、多色の明暗、入口の大きさである。

未評価の軸は次のとおりである。

- 利用者以外による意味の読み取り。キーボードの道具と読めるかは agent の推測にとどまる
- ブラウザの実タブでの 16px の見え方。確かめたのはモックの 16px だけである
- 多色版の黄の浮いたキーと、明の面とのコントラスト（1.61:1）。輪郭は墨の盤面と位置で補っている

## Decision

- 採用: `tilt-confetti`
- 判断者: 利用者
- 判断日: 2026-09-25
- 理由: 第 2 世代で利用者は `pop-tilt` と `pop-confetti` を「いい感じ」とした。第 3 世代で `tilt-confetti` を選んだ理由は示していない。

2026-09-25 に KeySync へ適用した。

| 面             | 実装                                              | 大きさ          |
| -------------- | ------------------------------------------------- | --------------- |
| favicon        | KeySync の `public/favicon.svg`                   | 32 の viewBox   |
| ヘッダー       | KeySync の `Logo`（`src/ui/components/Logo.tsx`） | 2.25rem（36px） |
| workspace 入口 | 同上                                              | 4rem（64px）    |

KeySync の画面では多色版を使い、黄の箱（`.logo` の背景）を外した。
マークの主役が黄の浮いたキーで、黄の箱の上では溶けるためである。
却下した案は、黄の箱に単色で置く案（Pop Toy の色がマークから消える）である。

favicon だけは SVG に色を書く。
タブには利用画面の CSS が届かないためである。
墨のキーは `prefers-color-scheme` で明は `#1f1f1f`、暗は `#f0f0f0` に切り替え、黄、青、赤橙は Pop Toy の値を固定で書く。

## Rejected reasons

2026-09-25 に利用者が `tilt-confetti` を採用したため、第 3 世代のほかの案を却下した。
案ごとの却下理由を利用者は示していない。
次は制作中に agent が観察した懸念であり、却下理由ではない。

| variant        | 制作中の観察（agent）                                             |
| -------------- | ----------------------------------------------------------------- |
| `pop-tilt`     | 盤面が墨 1 色で、Pop Toy の色は浮いたキーとスペースバーだけに出る |
| `pop-confetti` | 浮いたキーがまっすぐで、跳ねる動きが弱い                          |
| `ks-home`      | S と K の位置は配列を知る人にしか伝わらず、単色では差が消える     |
| `k-grid`       | 16px で 3px のキーが 6 個並び、K より賽の目に近く見える           |
| `k-legend`     | K の刻印が 16px と 24px で潰れ、favicon には専用版が要る          |

第 1、2 世代の却下は、Scope の比喩の経緯と「削除した variant」に記録した。

## Learnings

- 比喩の系統を 4 つ、各 2〜3 案の下書きで並べたところ、第 1 世代で方向が決まった。uiux-numa-logo は言葉で比喩を合意して 2 世代外れた。[比喩は描き方より先に効く](../../docs/principles/metaphor-decides-before-style.md) の手順 2〜3（系統の違う下書きを見せて選ぶ）を裏付ける 2 件目である。
- 第 2 世代では、1 軸ずつ変えた 7 案から 2 案が選ばれ、その合成が採用された。派生を 1 軸ずつに分けておくと、良い軸だけを取り出して組み合わせられる。
- 利用者は K と S を読ませる案を求めたが、名前の字を入れない合成案を採用した。uiux-numa-logo の「名前の文字を核にした形が先に選ばれた」とは逆の結果で、1 件の観察として残す。
- 32 の viewBox を 16px で描くと、奇数座標の面は半ピクセルにかかって滲んだ。面を主にするマークでも、`SVG-01` の格子は最初に決める。

## Related

- [uiux-numa-logo](../uiux-numa-logo/README.md): ロゴの Experiment の進め方と記録の手本
- [cornix-ui-icons](../cornix-ui-icons/README.md): KeySync の機能アイコン。キーキャップの枠と丸い端点の出どころ
- [比喩は描き方より先に効く](../../docs/principles/metaphor-decides-before-style.md): 比喩の系統を先に並べる手順の出どころ
