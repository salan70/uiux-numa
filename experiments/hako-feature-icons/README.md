---
title: Hako の機能アイコン
status: implementing
created: 2026-09-18
updated: 2026-09-18
platforms:
  - web
domains:
  - iconography
  - visual-design
  - consistency
  - accessibility
---

## Problem

Hako の LP の機能アイコン 3 個は、Feather 風の汎用のストロークをそのまま置いている。
既製に見え、Hako の性格を表していない。
AI エージェントが要求文から SVG アイコンを作る手順と知識（Skill）もなく、品質を再現できない。
この Experiment は Issue #7 の最初の実証として、次の 2 つを確かめる。

- 方向性の異なる案を作り、単体と利用画面内で比較し、改善前後を残す。
- 同じ要求文とモデルで Skill の有無を比べ、制作手法の効果と限界を記録する。

## Target

- 開発者: 個人開発で LP とアプリのアイコンを AI エージェントに依頼する。デザインの専門知識はない。
- 利用者: LP を初めて訪れ、機能の見出しとアイコンを 1 秒程度で流し読みする。アプリ内では 16px のアイコンをラベルと一緒に見る。

## Scope / Domains

対象領域は iconography、visual design、consistency、accessibility。
variant で変える軸は次の 2 つ。

- 造形の方向: 線の太さ、端点と角、塗りか線か、比喩の選び方
- 制作手法: Skill の有無、既存のアイコンをそのまま使う

意味（担当を決める、期限を知らせる、進み具合を見る）、24px の viewBox、単色、3 個 1 組は全 variant で共通にする。
利用画面のモック（`Mock.tsx`、`mock.css`）も全 variant で同一にし、`diff` で確認する。

## Constraints

- viewBox は `0 0 24 24`。単色で、fill と stroke は `currentColor` か `none` だけを使う。
- 自己完結した静的 SVG にする。`script`、外部参照、`text`、アニメーションを使わない。`just svg-check --mono --viewbox "0 0 24 24"` を通す。
- 3 個で見かけの大きさと線の太さを揃える。
- 利用画面は LP の機能セクション（40px の角丸の面の上に 24px）と、アプリ内のタスク詳細（16px、ラベル併記）。配色は color-schemes の `wasabi` のライト。
- 編集用の原本を `variants/<id>/source/`、配布用を `variants/<id>/dist/` に置く。配布用は `just svg-optimize` で作る。
- `index.tsx` は配布用を `?raw` で inline に展開する。
- モックは React + TypeScript で、追加の npm 依存を入れない。

Skill の有無を比べる variant には、次の要求文をそのまま渡す。
出力先の 1 行だけを variant ごとに置き換える。

```text
Hako は小さなチーム向けのタスク共有アプリです。その LP の機能セクションに置く機能アイコンを 3 個、1 組として SVG で作ってください。

- 機能と意味: 「担当を決める」（タスクごとに担当者を 1 人決める）、「期限を知らせる」（期限が近いタスクを前日に通知する）、「進み具合を見る」（チーム全体の完了率を週ごとに確認する）
- 利用画面: 各機能カードの見出しの上に 24px で置く。40px の角丸の面（配色 wasabi の --color-accent-subtle: #d6e9ca）の上に、--color-accent-strong（#006e54）を currentColor として受けて描く。アプリ内では 16px でも使う。
- 制約: viewBox は 0 0 24 24。単色で、fill と stroke は currentColor か none だけを使う。自己完結した静的 SVG にする（script、外部参照、text、アニメーションを使わない）。3 個で見かけの大きさと線の太さを揃える。
- 目指す印象: 小さなチームの実務に合う、落ち着いた印象。既製のアイコン集をそのまま使ったように見えないこと。
- 出力: 編集用の原本を <出力先>/source/assign.svg、deadline.svg、progress.svg として置く。配布用の最適化は行っても行わなくてもよい。最後に、造形の理由と確認した内容を報告する。
```

## Hypothesis

手順と知識（Skill）を与えると、既製のアイコン集に似た造形が減り、3 個の見かけの大きさと太さが揃い、16px でも識別できる。
造形の方向は印象を変えるが、意味の伝達は比喩の選び方に依存し、方向だけでは決まらない。
利用画面では、面の上の 24px と、ラベル併記の 16px で求められる細部の量が異なる。

## Variants

| id                  | 仮説                                                                                                                        | 変えた軸                                                      | 実装                          |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ----------------------------- |
| `current-feather`   | 基準: 既存の Feather 風の線画。既製に見え、Hako の性格を表さない                                                            | 制作手法 = 既存の SVG をそのまま使う                          | `variants/current-feather/`   |
| `no-skill`          | 基準: Skill なしでも要求文だけで実務的な組を作れるが、造形の根拠と一貫性は制作者の裁量に依存する                            | 制作手法 = Skill なし（`claude -p`）                          | `variants/no-skill/`          |
| `with-skill`        | 同じ要求文でも Skill があれば、造形の根拠が参照 ID で残り、16px のピクセル整列と組の統一が手順として保証される              | 制作手法 = Skill あり（`claude -p`）                          | `variants/with-skill/`        |
| `crisp-square`      | 定規で引いた事務用品（線幅 2、角張った端点、角丸なし、整数座標）の造形は、几帳面さと信頼を伝え、16px でも線と隙間が保たれる | 造形の方向 = 角張った線画 + 机上の道具の比喩                  | `variants/crisp-square/`      |
| `soft-solid`        | 線を使わず塗りのシルエットと負の空間だけで描くと、細部が減って 16px でも読め、面の上で落ち着いた存在感になる                | 造形の方向 = 塗り + 負の空間（角丸 外 2 / 内 1）              | `variants/soft-solid/`        |
| `hako-frame`        | 3 個が同じ「上が開いた箱」を共有し、意味の要素を箱の口から上へ出せば、組としての独自性と 16px の識別を両立できる            | 造形の方向 = 共通の箱のモチーフ（線幅 2、round）              | `variants/hako-frame/`        |
| `principle-applied` | 原則候補の手順に厳密に従って縛れば、由来と違うプロダクト、個数、サイズ帯でも組は揃う                                        | 制作手法 = 原則候補の手順に従う（比喩は `with-skill` と同じ） | `variants/principle-applied/` |

### Skill なし実行の条件

`no-skill` は、Skill `crafting-svg` がリポジトリに存在しない時点で、新しいプロセスの `claude -p` に要求文を渡して作った。

- 実行日: 2026-09-18
- モデル: `claude-fable-5-1`
- 権限: `--permission-mode acceptEdits`、`Bash` は `just`、`ls`、`cat`、`mkdir`、`resvg`、`svgo`、`xmllint`、`file` だけを許可
- 環境: リポジトリのルートで `nix develop -c` 経由。`CLAUDE.md`、この README、`current-feather`、`scripts/`、SVG 制作の ADR を読める状態
- 結果: 79 ターン、約 19 分、費用 7.35 USD。ツール呼び出し 77 回（Bash 41、Read 23、Write 12、Edit 1）
- session_id: `08f5138b-b6b2-4a03-b068-7ece1ae05fa9`

実行の記録から分かった行動は次のとおり。

- 既存の README、`current-feather`、`scripts/` の 3 本、SVG 制作の ADR を読み、`part-<asset>-<role>` の id と `role="img"` の規約を自力で採用した。
- `just svg-sheet` で 16、24、48px を描画し、3 回反復した。resvg で利用画面の色を再現した比較画像も作った（規則外の名前だったため保存していない）。
- Chrome での利用画面の撮影と `index.tsx` は作らなかった。`index.tsx` と in-context の preview は、担当者が同じモックで後から加えた。
- `just svg-check --viewbox` の引数が分割される不具合を報告した。recipe を修正した。

`no-skill` の造形の理由（実行の報告から要約）は次のとおり。

- stroke 1.5、round の端点。24px で線が軽く、16px で 1px になる。基準の Feather 風（2px）より細くして落ち着きに寄せた。
- 各アイコンに塗りの要素を 1 つだけ置き、「線画 + 1 点の塗り」で組として見せる。
- assign: 塗りの頭と左右対称の肩の弧に、右上のチェックで「決まった」を示す。deadline: 縁の線で閉じた鐘と塗りの舌。progress: 台の線に立つ 3 本のバーで、今週だけ塗り。高さは等間隔を避けて電波表示と区別した。
- 却下した案: 肩の弧をチェックへ続ける形（48px で波に見えた）、呼び鈴（縦長だとランプに見え、平たいと組の大きさが揃わない）、カレンダー + 日付の点（「知らせる」が消える）、折れ線と円グラフ（推移か完了率のどちらかが落ちる）。

### Skill あり実行の条件

`with-skill` は、Skill `crafting-svg` を追加した後に、同じ要求文の先頭に `/crafting-svg` を付けて新しいプロセスの `claude -p` に渡して作った。
出力先の 1 行と、仮置きのファイルの扱いと開発サーバーの案内の補足だけが `no-skill` と異なる。

- 実行日: 2026-09-18
- モデル: `claude-fable-5-1`
- 権限: `no-skill` と同じに加えて `nix develop`、`diff` を許可
- 結果: 93 ターン、約 16 分、費用 8.37 USD。ツール呼び出し 90 回（Bash 42、Write 24、Read 22、Agent 1、Edit 1）
- session_id: `52fad342-092b-4b99-83ca-a58930957e60`
- Skill の読み込み: `/crafting-svg` の展開で SKILL.md が読み込まれ、参照資料 5 本（icon、svg-authoring、foundations、self-review、ui-fit）と `docs/principles/README.md` を読んだ。造形の理由に知識 ID を引用し、Brief を知らない別 agent にラベルなしのシートを読ませる確認を自発的に行った。

### 方向の variant の制作条件

`crisp-square`、`soft-solid`、`hako-frame` は、方向のパラメータ（線幅、端点と角、塗りか線か、比喩の範囲、グリッド）を担当者が指定し、Skill を読み込んだ subagent に 1 variant ずつ任せた。
要求文は共通。反復は最大 3 回。利用画面の撮影まで subagent が行い、README への記録は担当者が写した。

### 反復の記録

`current-feather` は既存のため反復なし。`no-skill` の反復は実行の報告にだけ残り、round ごとの記録はない。

| variant        | round | 観察                                                                                                                                                                                   | 変更                                                                                                      | 参照                               | 終了理由                         |
| -------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ---------------------------------- | -------------------------------- |
| `with-skill`   | 1     | 16px はにじみなし。deadline だけ小さく軽い                                                                                                                                             | 胴を 1.5 伸ばし、舌を円から半円にした                                                                     | ICON-06、FORM-03                   | 指摘あり                         |
| `with-skill`   | 2     | 単体と利用画面で大きさは揃った。別 agent の目隠しの読み取りで、第一印象は 3 個とも意図どおり。指摘 3 点（assign が 16px で潰れる、deadline が軽い、progress の線と棒が無関係に見える） | なし                                                                                                      | ICON-02                            | 指摘あり                         |
| `with-skill`   | 3     | 読み取りの指摘 3 点                                                                                                                                                                    | assign の頭を r 3 に、題名の線を左辺から離す。deadline の舌を r 3 に。progress の棒の両端を到達線に揃える | FORM-04、ICON-08、FORM-10          | 上限到達。課題ありは残っていない |
| `crisp-square` | 1     | 暦の上端と見出し線が 16px で 1 本の帯に溶ける。点 + 横棒が人物ではなく文字行に見える。L 字が枠付きの 2 個より小さく見える                                                              | 暦の枠を 1 高くし見出しを y 10 へ。肩を札の下端から立ち上がる形へ。L 字を 20×19 に拡大                    | ICON-08、ICON-02、ICON-06、FORM-01 | 指摘あり                         |
| `crisp-square` | 2     | 幅 16 の名札が利用画面の 16px 行で他より細く小さく見える                                                                                                                               | 名札を幅 18 へ                                                                                            | ICON-06、FORM-02                   | 指摘なし                         |
| `soft-solid`   | 1     | 塗りの面積が 165 / 178 / 167 で鐘が 7% 重く、高さも最大。人物の重心が左寄り                                                                                                            | 鐘の壁を 1 短くし全体を 1 下げ、面積 166 に                                                               | FORM-03、ICON-06、FORM-01          | 指摘あり                         |
| `soft-solid`   | 2     | 利用画面の 40px の面で人物が左に寄り、円グラフが左下に沈む                                                                                                                             | 人物の肩を幅 12 に狭めチェックを首の高さへ。円グラフの中心を (12.5, 11.5) へ                              | FORM-01、ICON-06                   | 指摘あり                         |
| `soft-solid`   | 3     | 欠けの 2 辺が 24px でグレーに割れる                                                                                                                                                    | 欠けの辺を x 13、y 11 の整数線に置き、半径を 8.25 に                                                      | ICON-09、SVG-01、FORM-03           | 上限到達。課題ありは残っていない |
| `hako-frame`   | 1     | assign の肩が 16px で塊になり人に見えない。progress の棒 3 本が壁と合わせて櫛になる。全体が 1px 上寄り                                                                                 | 箱を 1 下げ、肩を底から浮かせ、頭を cy 6 へ。棒を 2 本に                                                  | ICON-08、ICON-11、FORM-01、FORM-04 | 指摘あり                         |
| `hako-frame`   | 2     | 利用画面の 16px で progress が漢字の「山」に見える。6 案を比較し、縦棒を持つ案はすべて「山」、上矢印は upload、塗り帯は縦の見かけが半分になる                                          | 棒をチェック（`M8 13L11 16L16 6`）に置き換え                                                              | ICON-02、ICON-03、ICON-06          | 指摘あり                         |
| `hako-frame`   | 3     | 16px で人、「!」、チェックが取り違えない。縦の外形が揃う。面の上で 2 個が 1px 弱下寄りに見えるが許容                                                                                   | なし                                                                                                      | ICON-11、FORM-01                   | 指摘なし                         |

### 方向ごとの造形の理由と未解決の点

`with-skill`（線幅 1.5、round、線で器を描き塗りで決まったものを 1 つ置く）

- 線の中心を 0.75 + 1.5n に置き、16px（2/3 倍）で整数ピクセルに乗せた（SVG-01、ICON-09）。線幅 2 は 16px で 1.33px になりにじむため採らなかった。
- assign はカードの線に担当者の塗り、deadline は鐘の線に舌の塗り、progress は 100% の到達線に完了分の塗り。既製の「人 + チェック」の配置を避けた。
- 却下: 挙手する人（腕が斜線に見える）、台座の上の人（判子に見える）、カレンダー + 鐘（16px で assign と取り違える）、棒 + 幹（スライダーに見える）。下書きは `previews/with-skill-sketch-metaphor-sheet.png`。
- 未解決: deadline は目隠しの読み取りで「Feather や Lucide の鐘に似る」と指摘され、比喩の限界として許容した。progress の到達線は「上限」や「見出し線」と読まれる可能性が残る。

`crisp-square`（線幅 2、butt と miter、角丸なし、整数座標）

- 塗りは各 1 か所、4 単位の要素（頭、印の日、今週の棒）だけに置き、「決まった 1 つ」を示す（ICON-10、FORM-08）。
- assign は名札に頭と肩、deadline は卓上の暦に印の日、progress は L 字の軸に立つ週ごとの棒。「知らせる」は比喩に含めずラベルに委ねた（ICON-01）。
- 許容: 暦は水平線 3 本を持つため 3 個の中でわずかに重い（FORM-06）。名札の肩の内側は 16px で灰色の面になる。
- 未解決: SVGO が既定値の `stroke-linecap="butt"` と `stroke-linejoin="miter"` を配布用から落とす。利用先の CSS が round を指定すると角が変わる。

`soft-solid`（線なし、塗りと負の空間、角丸 外 2 / 内 1）

- 塗りの面積を 96px 描画の alpha 合計で測り、157 / 166 / 166 に揃えた（FORM-03）。円グラフは keyline 20 より小さい直径 16.5 で、面積の一致を優先した。
- assign は頭と肩のシルエットに塗りのチェック、deadline はドームと広がる裾の鐘に半円の舌、progress は右上が欠けた円グラフ（欠け = 残り、閉合で円として読む。FORM-10）。
- 却下: 3 個すべてを角丸四角の切り抜きにする案（40px の面の上で重すぎ、切り抜きが 16px で 1.3px を割る）。
- 未解決: 塗りは既定の線画より強調として働くため（ICON-10）、本文の横で文字より重く見える可能性がある。複数行の一覧では未検証。

`hako-frame`（線幅 2、round、共通の上が開いた箱）

- 箱は外形 18×10（x 3〜21、y 11〜21）で幅を担い、意味の要素が口から上へ出て高さを担う。16px で箱が容器として読める最小の高さを確保しつつ、要素の輪郭が識別を担う（ICON-06、ICON-11、FORM-09）。
- assign は箱の中の人、deadline は箱に入る「!」、progress は箱の中のチェック。
- 却下: 進捗を「箱が満たされる度合い」（底の塗り帯）で表す案は、縦の見かけが他 2 個の半分になり ICON-06 に反する。
- 未解決: 「!」がエラーや警告に、チェックが「完了する操作」に読まれる可能性がある。3 個中 2 個だけに塗りがあり、progress は線のみ。チェックは task アプリで頻出の形で、箱との組み合わせで固有性が出るかは評価に委ねる。

### 原則候補の別プロダクトでの試行

`class-tech-icons`（授業資料サイト、8 個 1 組、20〜96px）から抽出した原則候補「[組の一貫性は少数のパラメータで縛る](../../docs/principles/icon-set-consistency-by-few-parameters.md)」を、この Experiment（架空のタスク共有アプリ、3 個 1 組、16〜24px）で試した。
プロダクトの性格、個数、サイズ帯、配色がすべて違う。

variant `principle-applied` は、原則の初版の手順に厳密に従って作った。
比喩は `with-skill` と同じ 3 つ（カードと担当者、鐘と舌、到達線と棒）にし、変えたのはパラメータの守り方だけである。
造形の作り込みではなく原則の検証が目的のため、反復は 2 回で止めた。

4 案を同じ測り方で比べた結果は次のとおり。

| 項目                                | `no-skill`                       | `with-skill`                    | `crisp-square` | `principle-applied`  |
| ----------------------------------- | -------------------------------- | ------------------------------- | -------------- | -------------------- |
| root の属性が 3 個で一致            | 一致                             | 一致                            | 一致           | 一致                 |
| 線幅と端点の要素側の上書き          | 0                                | 0                               | 0              | 0                    |
| 座標の刻み（種類）                  | 整数 15、0.5 刻み 5、他の小数 15 | 整数 8、0.5 刻み 7、0.75 刻み 9 | 整数 20 のみ   | 整数 22 のみ         |
| 24px の外形の範囲（最大と最小の比） | 17〜20（1.18）                   | 18×18 で一致（1.00）            | 18〜20（1.11） | 20×20 で一致（1.00） |
| 塗りの要素数                        | 3                                | 6                               | 3              | 0                    |
| 16px の中間 alpha 画素の割合        | 78%                              | 41%                             | 66%            | 89%                  |

判定は**部分的に反証**である。

- 支持された点: 24px の外形が 3 個とも一致し、座標は整数だけになった。外形の一致は別プロダクトでも再現した。
- 反証 1: 初版が固定するとした 3 パラメータのうち、root の属性の一致と要素側の上書き 0 は、4 案すべてが既に満たしていた。Skill も原則も使わない `no-skill` でも満たしている。差を生んだのは座標の刻みと外形だけだった。
- 反証 2: 外形の一致は 3 パラメータの必要条件ではない。`with-skill` も一致を達成しており、語彙を混ぜ、座標も整数ではない。
- 反証 3: 初版は最小の表示サイズを決めていなかった。24 の viewBox の整数座標は 16px で境界から外れ、線幅 2 も 1.33px になる。結果、16px のにじみが 4 案で最悪になった。由来の Experiment が 20〜96px だったため、この穴は露出しなかった。

この反証を受けて、原則候補に「最小の表示サイズを先に決め、線幅と刻みをそこから逆算する」手順と、内側の最小隙間を足した。
`principle-applied` の 16px のにじみは、原則の初版の値を守る限り直せない。値を変えると `with-skill` と同じになり、手順に厳密に従った検証でなくなる。この矛盾は残した。

preview は `previews/principle-applied-final-sheet.png`、`previews/compare-principle-24.png`、`previews/principle-applied-final-in-context.png`。

### previews

- `previews/<id>-first-sheet.png`: 最初の完成形の比較シート（`with-skill`、方向の 3 variant）
- `previews/<id>-final-sheet.png`: 配布用で描画した最終の比較シート（全 variant）
- `previews/<id>-final-in-context.png`: 利用画面のモック（全 variant）。1280×900、倍率 2
- `previews/compare-first-sheet.png`: 改善前の比較。各 variant の first-sheet を縦に並べた。基準 2 件は final を載せた
- `previews/compare-final-sheet.png`: 改善後の比較。アイコンごとに 6 variant を並べた

## Evaluation

未定

## Decision

未定

## Rejected reasons

未定

## Learnings

未定

## Related patterns / assets

なし
