---
title: UI/UX NUMA のロゴ
status: implementing
role: module
maturity: experimental
created: 2026-09-22
updated: 2026-09-23
platforms:
  - web
domains:
  - logo-brand-identity
  - visual-design
  - accessibility
sources:
  - color-schemes-material
  - product-ui-typography
adopted: []
---

## Problem

公開サイトの表示名を「UI/UX NUMA」へ変えたが、顔になる形がない。
[表示名の ADR](../../docs/decisions/2026-09-22-display-name-uiux-numa.md) は「ロゴの wordmark は本 ADR では決めない」「ロゴと favicon は未着手」を残した。

favicon は存在しない。
`apps/catalog/index.html` に `<link rel="icon">` がなく、`apps/catalog/public/` は `_redirects` だけである。
本番 `https://uiux.oda79.me/` はブラウザ既定の白紙アイコンで出ている。

サイドバー題字とホーム大見出しは、書体そのままの文字列である。
タブ、ブックマーク、共有先のように文字が使えない面で、このサイトを指す形がない。

## Target

Catalog を見る利用者と、リポジトリの作者自身を対象にする。
最も頻度が高い面はブラウザのタブ（16px）である。
次がサイドバー題字（20px 相当）、最も大きい面がホームの大見出し（42〜96px）である。

## Scope / Domains

対象はマーク 1 個と、その lockup の形式である。
domain は `logo-brand-identity`、`visual-design`、`accessibility`。
`iconography` は入れない。`logo-brand-identity` に topic が無いため、2 番目以降の domain が topic を決めてしまう。

variant で変えるのは「文字のどの字形を核にし、何を結び付けるか」だけである。
viewBox、live area、格子、字画、色数は 5 案で共通にする。

比喩の系統と目指す印象は、制作前に利用者が決めた（2026-09-22、2026-09-23）。

- 比喩: 初回は「比較の 2 面」だった。3 案とも却下され、「沼」から連想される水や沼地の気配を少し入れることへ変えた。
- 比喩（第 3 世代）: 水の気配の 4 案も却下された。理由は「比喩が合わない」と「文字要素が欲しい」である。全案に文字（N、U、nu）を核として入れ、文字、有機的な沼の形、UI/UX の記号、抽象的な記号の 4 系統で作る。
- 印象: 実験的で動きがある。したがって非対称にする（`LOGO-03`）。
- 作図: 座標は数式から出す。体系、理論値、採用値、誤差を残す。

この順は [比喩は描き方より先に効く](../../docs/principles/metaphor-decides-before-style.md)（`candidate`）と、`crafting-svg` の手順 1 に従う。
削除した `class-doc-logo` は比喩をエージェントが選び、4 案とも却下された。

## Constraints

- `<text>`、`var()`、`aria-labelledby` を使わない。`role="img"` と最初の子 `<title>`、id は `part-<asset>-<role>`、色は `currentColor`（[SVG ツールチェーンの ADR](../../docs/decisions/2026-09-18-svg-toolchain.md)）。
- フォントの outline 化ツールは同 ADR で却下済みである。path の wordmark を作る場合、LINE Seed JP の複製ではなく独自の作図になる。
- SVG に色を書かない。差し替えは利用画面の CSS で `part-*` を上書きする。
- 白黒 1 色で設計し、反転と 16px で成立してから色を考える（`LOGO-07`）。
- 10 配色 × light / dark で成立させる。`sumi` の dark は `--cat-accent` が背景にほぼ溶けるため、前景に色を持たせるなら `--cat-accent-text` を使う。
- viewBox は `0 0 32 32`。favicon の基準が 32px である（`LOGO-07`）。
- Catalog には載せない。`domains` の先頭を `logo-brand-identity` にして topic を持たせない（[掲載しない Experiment の ADR](../../docs/decisions/2026-09-22-unlisted-experiments.md)）。

## Hypothesis

NUMA の頭文字（N）か、UI/UX と NUMA に共通する字（U、nu）を核にすると、16px から 96px まで同じ形で成立し、かつ既製のアイコン集と区別できる固有の形になる。

## Variants

| id           | 仮説                                                                                  | 変えた軸           | 実装                   |
| ------------ | ------------------------------------------------------------------------------------- | ------------------ | ---------------------- |
| `n-monogram` | 縦画の天地を字画 1 本分ずらした N は、既製の書体と区別できる固有の頭文字になる        | 文字だけ           | `variants/n-monogram/` |
| `nu-arches`  | 180° 回転の関係にある n と u を 1 本の線でつなぐと、NUMA の字の律動を抽象記号にできる | 文字の律動（抽象） | `variants/nu-arches/`  |
| `n-pond`     | 小文字 n の脚が沼へ浸かる形は、文字と沼の気配を同時に持てる                           | 文字 + 沼の形      | `variants/n-pond/`     |
| `u-basin`    | U の底に水をため泡を浮かべると、UI/UX と NUMA に共通する U を沼の窪地にできる         | 文字 + 沼の形      | `variants/u-basin/`    |
| `cursor-n`   | N の斜画をポインタに置き換えると、UI/UX の実験場であることと頭文字を同時に示せる      | 文字 + UI の記号   | `variants/cursor-n/`   |
| `overview`   | 基準: 5 案を 1 ページに並べると、最小サイズと lockup を同じ条件で見比べられる         | 5 案の一覧         | `variants/overview/`   |

`overview` は比較案ではなく一覧面である。採否の対象にしない。

4 字の path wordmark（`numa-wordmark`）は作らない。32 角の favicon に入らず、lockup 形式 B の比較はマークの選定後に行うためである。

### 造形のパラメータ

5 案で共通にする。

| 項目      | 値                | 理由                                                                 |
| --------- | ----------------- | -------------------------------------------------------------------- |
| viewBox   | `0 0 32 32`       | favicon の基準が 32px（`LOGO-07`）                                   |
| live area | `4..28`（24 角）  | 外周 4 を余白に残す。`LOGO-06` の clear space をマーク内に先取りする |
| 格子      | 偶数座標          | 16px（1/2 倍）で整数ピクセルに乗る                                   |
| 字画      | 4 = 24 / 6        | 16px で 2px。第 2 世代の線幅 2（16px で 1px）では字が細く読めない    |
| 色数      | 1（currentColor） | 白黒 1 色で始める（`LOGO-07`）                                       |
| 対称軸    | なし              | 実験的で動きがある印象（`LOGO-03`）                                  |

### 座標の導出

座標は目分量で置かず、体系から理論値を出し、格子へ丸め、誤差を残す。

`n-monogram`

| 対象           | 体系                          | 理論値                       | 採用値 | 誤差   | 理由                             |
| -------------- | ----------------------------- | ---------------------------- | ------ | ------ | -------------------------------- |
| 縦画の幅       | 字画                          | 4                            | 4      | 0      | 5 案で共通                       |
| 縦画の天地ずれ | 字画 1 本分                   | 4                            | 4      | 0      | 左 8..28、右 4..24。点対称を崩す |
| 斜画の横幅     | 垂直な太さを字画 4 に合わせる | 4 × √(18² + 16²) / 16 = 6.02 | 6      | −0.35% | 垂直な太さは 3.986               |

`nu-arches`

| 対象         | 体系                         | 理論値 | 採用値 | 誤差 | 理由                                            |
| ------------ | ---------------------------- | ------ | ------ | ---- | ----------------------------------------------- |
| 肩と底の半径 | 2:3、直径の和が中心線の幅 20 | 4, 6   | 4, 6   | 0    | 8 + 12 = 24 − 4。半径が違うので点対称にならない |
| 共有する縦画 | n の直径                     | 8      | 8      | 0    | y 12..20                                        |
| 脚の端       | live area の天地             | 28, 4  | 28, 4  | 0    | 左は底から、右は天まで伸ばす                    |

`n-pond`

| 対象       | 体系                  | 理論値         | 採用値 | 誤差 | 理由                                   |
| ---------- | --------------------- | -------------- | ------ | ---- | -------------------------------------- |
| 肩の半径   | 天 4 に外縁を合わせる | 11 − 4 − 2 = 5 | 5      | 0    | 脚の中心 10 と 20                      |
| 水面の隙間 | 字画の半分            | 2              | 2      | 0    | 脚は y 20 で切り、沼は 22 から         |
| 沼の丈     | live area の 1/4      | 6              | 6      | 0    | 両端は半径 3 の半円                    |
| 水面下の脚 | 沼の丈の半分          | 3              | 3      | 0    | 脚の幅で沼を抜き、脚が水中へ続くと示す |
| n のずれ   | 格子 1 単位           | 1              | 1      | 0    | n の中心 15 と沼の中心 16 をずらす     |

`u-basin`

| 対象         | 体系                                | 理論値          | 採用値 | 誤差 | 理由                                |
| ------------ | ----------------------------------- | --------------- | ------ | ---- | ----------------------------------- |
| 底の半径     | 外縁を live area の底 28 に合わせる | 28 − 2 − 17 = 9 | 9      | 0    | 中心 (16,17)                        |
| 右の脚の短縮 | live area の 1/3                    | 8               | 8      | 0    | 左は天 4、右は y 12                 |
| 水の半径     | 内縁 7 に字画へ 1 重ねる            | 8               | 8      | 0    | 境目の髪線を消す（round 3）         |
| 泡           | 半径 2、水面から隙間 3              | 17 − 3 − 2 = 12 | 12     | 0    | 中心から 2 左へずらし、対称軸を消す |

`cursor-n`

| 対象           | 体系                   | 理論値      | 採用値 | 誤差   | 理由                                         |
| -------------- | ---------------------- | ----------- | ------ | ------ | -------------------------------------------- |
| 縦画           | 字画、live area の天地 | 幅 4、4..28 | 同     | 0      | N の枠                                       |
| ポインタの先端 | 縦画と天から字画の半分 | (10, 6)     | 同     | 0      | 縦画とポインタを別の形として読ませる         |
| 頭と切り欠き   | 45°                    | 傾き 1      | 1      | 0      | OS のポインタの頭の角度                      |
| 尾の太さ       | 傾き 2 で字画 4 を狙う | 4           | 3.58   | −10.6% | 格子に乗る横幅 4 を優先した（可読性 > 比例） |

### 反復の記録

| round | 観察                                                                         | 変更                                                        | 参照                 | 終了理由               |
| ----- | ---------------------------------------------------------------------------- | ----------------------------------------------------------- | -------------------- | ---------------------- |
| 1     | `offset-pair` は手前の塗りが奥の輪郭を覆い、比較ではなく複製に読めた         | 2 面とも輪郭にし、重なりだけを塗る                          | `LOGO-04`            | 複製の読みが消えた     |
| 1     | `chosen-one` は幅が同じで底辺が揃い、棒グラフに読めた                        | 幅と丈の両方を変え、天地も揃えない                          | `LOGO-03`            | 図表の読みが消えた     |
| 1     | `split-face` は対角線が 16px で灰色に滲む                                    | 変更なし                                                    | `LOGO-07`            | 未解決として残す       |
| 2     | 利用画面で見ると、`offset-pair` は 16px で 2 面が 1 つの塊に潰れる           | 変更なし。マークの選定に持ち込む                            | `LOGO-01`            | 判断待ち               |
| 2     | `chosen-one` は矩形 2 個だけで固有の要素が弱い                               | 変更なし。マークの選定に持ち込む                            | `LOGO-04`            | 判断待ち               |
| 2     | `split-face` の割れ目が `N` の字面に見え、`NUMA` と結び付く                  | 変更なし                                                    | `LOGO-04`、`LOGO-08` | 判断待ち               |
| 3     | 3 案とも却下された。水や沼地の気配を入れ、座標を数式から出すことが求められた | 比喩を作り直し、4 案を新規に作る                            | `LOGO-02`            | 前提が変わった         |
| 3     | `water-line` は塗りの下半分と輪郭の上半分が保存アイコンに読めた              | 案を捨て、`reflection`（実体と映り込み）へ置き換える        | `LOGO-02`            | 誤読が消えた           |
| 3     | `ripple-rings` の等比 1/2（12, 6, 3）は隙間が 1px になり、16px で輪が潰れた  | 隙間を 3 で一定にする体系へ変え、半径を 11, 6, 2 にする     | `LOGO-07`            | 隙間が 1.5px 残った    |
| 4     | 水の 4 案も却下された。比喩が合わず、文字要素が求められた                    | 文字を核にした 5 案を新規に作る。字画を 2 から 4 へ太くする | `LOGO-02`            | 前提が変わった         |
| 4     | `cursor-n` の頭が直角二等辺で、ポインタではなく「上へ戻る」矢印に読めた      | 左辺を縦、右辺を 45° にし、切り欠きと尾を持つ形へ変える     | `LOGO-02`            | ポインタに読めた       |
| 4     | `n-pond` は沼が台座に見え、全体が呼び鈴に読めた                              | 脚と沼の間に水面の隙間を空け、水面下の脚を沼から抜く        | `LOGO-02`            | 磁石の読みが残った     |
| 4     | `u-basin` は半円の水と泡が口と目になり、笑顔に読めた                         | 水を底いっぱいに広げ、泡を左へ寄せる                        | `LOGO-02`            | 片目の顔の読みが残った |
| 5     | `u-basin` は水と U の境目に髪線が出た                                        | 水の半径を内縁 7 から 8 へ広げ、字画に重ねる                | `LOGO-07`            | 髪線が消えた           |

### 削除した variant

2026-09-22 に利用者が 3 案とも却下した。コードは削除し、仮説と軸だけを残す。

- `offset-pair`: 同形の 2 面をずらし、重なりだけを塗る。軸は 2 面の関係（重ね）。
- `chosen-one`: 幅と丈の違う 2 面の片方だけを塗る。軸は 2 面の関係（選択）。
- `split-face`: 1 つの面を非対称な対角で割る。軸は 2 面の関係（分割）。

2026-09-23 に利用者が水の 4 案も却下した。理由は「比喩が合わない」と「文字要素が欲しい」である。

- `reflection`: 水面に載る実体と、√2 で縮んでずれた映り込み。軸は水の表し方（像）。
- `wave-345`: 半径 3, 4, 5 の半円をつないださざ波。軸は水の表し方（波）。
- `ripple-rings`: 隙間を 3 で一定にした波紋。軸は水の表し方（環）。
- `sunken-pair`: 幅と同じ丈で、1:2 の深さまで沈む 2 本の柱。軸は水の表し方（沈）。

### previews

- `compare-first-sheet.png`: 却下した 3 案の初回。16 / 32 / 48px、明暗、最小サイズの 4 倍拡大。
- `compare-second-sheet.png`: 却下した 3 案の round 1 の変更後。同じ条件。
- `compare-water-sheet.png`: 却下した水の 4 案。同じ条件。
- `compare-second-in-context.png`: 却下した水の 4 案の一覧面。
- `compare-letter-sheet.png`: 現行 5 案。同じ条件。上から `n-monogram`、`nu-arches`、`n-pond`、`u-basin`、`cursor-n`。
- `compare-third-in-context.png`: 現行 5 案の一覧面（`variants/overview/`）。タブの 16px、縮小列、lockup、明暗の反転、見出しの大きさ。

## Evaluation

未定

## Decision

未定

## Rejected reasons

未定

## Learnings

未定

## Related patterns / assets

- [表示名の ADR](../../docs/decisions/2026-09-22-display-name-uiux-numa.md): 「UI/UX NUMA」と `NUMA` 単体の誤読という残る論点
- [掲載しない Experiment の ADR](../../docs/decisions/2026-09-22-unlisted-experiments.md): この Experiment を Catalog に載せない根拠
- [比喩は描き方より先に効く](../../docs/principles/metaphor-decides-before-style.md): 比喩を先に決める手順の出どころ
