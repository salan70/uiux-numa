#!/bin/bash
# このファイルは Git 管理対象です。秘匿情報を書かないでください。

# flake.nix の devShell を有効化する（nix と nix-direnv が必要）。
use flake

# マシン固有の設定（git アカウント切替など）は .envrc.local に書く（gitignore 対象）。
source_env_if_exists .envrc.local
